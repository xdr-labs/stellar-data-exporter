# Install under /etc/nginx/conf.d/ so this directive is loaded in http{}.
# Progress polling is ~2 requests/second; 10r/s leaves headroom while limiting abuse.
limit_req_zone $binary_remote_addr zone=stellar_exporter_api:10m rate=10r/s;
